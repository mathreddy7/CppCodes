#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int q;cin>>q;
    while(q--){
        string s,t;
        cin>>s>>t;
        int a=0;
        if(s.length()>t.length()){
            int k=t.lengthI();
            for(int i=0;i<k;i++){
                
            }
        }
    }
}   